\input texinfo    @c -*- texinfo -*-
@c %**start of header
@setfilename agents.info
@settitle agents: AI coding agent integration for Emacs
@documentencoding UTF-8
@documentlanguage en
@c %**end of header

@dircategory Emacs misc features
@direntry
* Agents: (agents). AI coding agent integration for Emacs.
@end direntry

@finalout
@titlepage
@title agents: AI coding agent integration for Emacs
@author Pablo Stafforini (@email{pablo@@stafforini.com})
@end titlepage

@contents

@ifnottex
@node Top
@top agents: AI coding agent integration for Emacs

This manual describes the features and customization options for the Emacs Lisp package @samp{agents.el}.

@end ifnottex

@menu
* Overview::
* User options::
* Commands::
* Functions::
* Transient menu::
* Indices::

@detailmenu
--- The Detailed Node Listing ---

User options

* Shared session behavior::
* Claude Code backend::
* Codex backend::

Commands

* Shared commands::
* Claude Code commands::
* Codex commands::

Functions

* Backend registration and display::
* Modeline and status helpers::

Indices

* Function index::
* Variable index::

@end detailmenu
@end menu

@node Overview
@chapter Overview

@samp{agents.el} collects the Emacs integration layer for AI coding command-line tools. It provides a shared session model, a unified transient menu, notification handling, terminal helpers, and backend modules for Claude Code and Codex.

The package is split into a small core and backend modules. The core file, @samp{agents.el}, is backend-agnostic: it knows how to register AI tools, find their buffers, dispatch shared commands, render backend icons, assign stable session keys, and present a single menu. @samp{agents-claude.el} integrates @samp{claude-code.el} with Claude-specific account switching, status polling, usage polling, branch navigation, batch TODO processing, and hook setup. @samp{agents-codex.el} integrates @samp{codex.el} with Codex-specific theme sync, skill execution, handoff, auditing, debugging, and modeline helpers.

The main workflow is to load @samp{agents.el}, then require the backend modules you use. Once the Claude and Codex modules are loaded, @samp{M-x agents-menu} and @samp{M-x agents-start-or-switch} operate across both tools. Sessions are grouped by account when the backend exposes account metadata, highlighted when waiting for input, and disambiguated by project and instance name when several sessions share a directory.

The package depends on @samp{transient} for menus and @samp{consult} for completion. The Claude backend expects @samp{claude-code.el}. The Codex backend expects @samp{codex.el}. Optional integrations use packages such as @samp{yasnippet}, @samp{doom-modeline}, @samp{gptel}, @samp{agent-log}, and @samp{monet} when those are available.

@node User options
@chapter User options

@menu
* Shared session behavior::
* Claude Code backend::
* Codex backend::
@end menu

@node Shared session behavior
@section Shared session behavior

@vindex agents-protect-buffers
@vindex agents-alert-on-ready
@vindex agents-alert-style
@vindex agents-alert-sound
@vindex agents-alert-sound-player
@vindex agents-backtrace-file
@vindex agents-sync-theme
@vindex agents-sigwinch-delay
The user option @code{agents-protect-buffers} is a boolean that defaults to @code{t}. When enabled, killing a live AI session buffer prompts for confirmation through @code{agents-protect-buffer}. Buffers whose processes have already exited are allowed to die without the prompt.

The user option @code{agents-alert-on-ready} is a boolean that defaults to @code{nil}. When enabled, backend notification handlers can call @code{agents-notify} to emit visual and/or audible alerts when a session is waiting for input. @code{agents-alert-style} selects @code{visual}, @code{sound}, or @code{both}; it defaults to @code{both}. @code{agents-alert-sound} defaults to @code{nil}; set it to a sound file to enable sound alerts. @code{agents-alert-sound-player} optionally names an external sound player when Emacs sound support is unavailable.

The user option @code{agents-backtrace-file} controls where @code{agents-save-backtrace}, @code{agents-claude-debug-backtrace}, and @code{agents-codex-debug-backtrace} write Emacs backtraces. It defaults to a file in @code{temporary-file-directory}.

The user option @code{agents-sync-theme} is a boolean that defaults to @code{nil}. When enabled, theme changes in Emacs are persisted through each registered backend's theme-sync handler. This updates configuration files for new or restarted CLI sessions without sending theme commands into active terminal conversations.

The user option @code{agents-sigwinch-delay} is a number of seconds and defaults to @samp{0.5}. It controls how long @code{agents-fix-rendering} waits before sending @samp{SIGWINCH} to a terminal process. Increase it if an AI CLI reads the terminal size before the @samp{eat} window has settled.

Snippet helpers use @samp{yasnippet} and @samp{consult-yasnippet} only when those optional packages are loaded.

@node Claude Code backend
@section Claude Code backend

@vindex agents-claude-warn-kill-with-branches
@vindex agents-claude-fork-worktree-directory
@vindex agents-claude-log-directory
@vindex agents-claude-status-interval
@vindex agents-claude-usage-interval
@vindex agents-claude-usage-max-interval
@vindex agents-claude-accounts
@vindex agents-claude-account-file
@vindex agents-claude-settings-file
@vindex agents-claude-hook-wrapper
@vindex agents-claude-statusline-script
@vindex agents-claude-status-directory
@vindex agents-claude-handoff-file
@vindex agents-claude-batch-allowed-tools
@vindex agents-claude-batch-permission-mode
@vindex agents-claude-batch-max-turns
@vindex agents-claude-batch-system-prompt
@vindex agents-claude-batch-model
@vindex agents-claude-run-skill-model
@vindex agents-claude-audit-skills
@vindex agents-claude-audit-project-directories
@vindex agents-claude-org-todo-in-progress-keyword
@vindex agents-claude-debug-backtrace-model
@vindex agents-claude-debug-backtrace-backend
The Claude backend options control account selection, session metadata, non-interactive execution, and branch workflows. @code{agents-claude-accounts} is an alist of account names to Claude configuration directories, and @code{agents-claude-account-file} stores the selected account across Emacs sessions. Use @code{agents-claude-select-account} or the account infix in @code{agents-menu} to switch accounts before starting a new session.

Status and usage polling are controlled by integer intervals. @code{agents-claude-status-interval} defaults to @samp{5} seconds and reads status JSON emitted by the bundled statusline script. @code{agents-claude-usage-interval} defaults to @samp{300} seconds and backs off up to @code{agents-claude-usage-max-interval}, which defaults to @samp{900} seconds.

Batch execution options are passed to @samp{claude -p}. @code{agents-claude-batch-permission-mode} defaults to @samp{"auto"}, @code{agents-claude-batch-max-turns} defaults to @samp{30}, and the allowed tool, model, and appended-system-prompt options let you narrow or expand what non-interactive Claude tasks may do.

Branch and buffer safety options default conservatively. @code{agents-claude-warn-kill-with-branches} defaults to @code{t} and warns before killing a session that belongs to a branch tree. @code{agents-claude-fork-worktree-directory} defaults to a cache directory so isolated branch worktrees do not interfere with cloud sync. @code{agents-claude-handoff-file} defaults to a file in @code{temporary-file-directory}.

Claude hook setup is explicit. @code{agents-claude-settings-file} names the settings file updated by @code{agents-claude-setup-config} and the individual ensure commands. @code{agents-claude-hook-wrapper} points to the upstream @samp{claude-code} hook wrapper, while @code{agents-claude-statusline-script} points to the bundled statusline helper. @code{agents-claude-status-directory} controls where the helper writes per-buffer status JSON files. Loading the backend does not modify Claude Code configuration.

@node Codex backend
@section Codex backend

@vindex agents-codex-handoff-file
@vindex agents-codex-accounts
@vindex agents-codex-account-file
@vindex agents-codex-skill-directories
@vindex agents-codex-audit-skills
@vindex agents-codex-audit-project-directories
@vindex agents-codex-exec-approval-policy
@vindex agents-codex-exec-sandbox-mode
@vindex agents-codex-exec-skip-git-repo-check
@vindex agents-codex-debug-backtrace-model
@vindex agents-codex-debug-backtrace-backend
The Codex backend options control account selection, handoff, skill discovery, non-interactive execution, project auditing, and backtrace debugging. @code{agents-codex-accounts} is an alist of account names to Codex home directories, and @code{agents-codex-account-file} stores the selected account across Emacs sessions. Account homes keep credentials such as @samp{auth.json} separate, while shared Codex state such as @samp{config.toml}, @samp{hooks.json}, skills, sessions, and @samp{history.jsonl} is symlinked from @samp{~/.codex/} so hooks, skills, project trust, and chat history remain available after switching accounts. Use @code{agents-codex-select-account} or the Codex account infix in @code{agents-menu} to switch accounts before starting a new session. @code{agents-codex-handoff-file} defaults to a file in @code{temporary-file-directory} and is read by @code{agents-codex-handoff} after the Codex handoff skill writes next-session instructions.

@code{agents-codex-skill-directories} is a list of additional directories scanned for Codex skills. The backend also scans standard Codex skill locations and project-local skill directories. @code{agents-codex-audit-skills} defaults to the code, design, and interpretability audit skills, while @code{agents-codex-audit-project-directories} stores completion candidates for the audit directory prompt.

@code{agents-codex-exec-approval-policy}, @code{agents-codex-exec-sandbox-mode}, and @code{agents-codex-exec-skip-git-repo-check} configure the non-interactive Codex calls used by skill and audit workflows. The approval policy is passed as a top-level Codex CLI option before the @samp{exec} subcommand, while the sandbox and repository-check options are passed to @samp{codex exec} itself.

@code{agents-codex-debug-backtrace-model} and @code{agents-codex-debug-backtrace-backend} configure the @samp{gptel} request used by @code{agents-codex-debug-backtrace} to identify likely culprit packages from an Emacs backtrace.

@node Commands
@chapter Commands

@menu
* Shared commands::
* Claude Code commands::
* Codex commands::
@end menu

@node Shared commands
@section Shared commands

@findex agents-menu
@findex agents-start-or-switch
@findex agents-jump-to-waiting
@findex agents-run-skill
@findex agents-post-push-ci
@findex agents-handoff
@findex agents-audit-project
@findex agents-debug-backtrace
@findex agents-exit
@findex agents-restart
@findex agents-toggle-alert
@findex agents-fix-rendering
@findex agents-disable-scrollback-truncation
@findex agents-snippet-tab
The command @code{agents-menu} opens the unified transient menu. It includes shared entries for session switching, waiting-session jumps, handoff, restart, exit, skill execution, project audit, backtrace debugging, alert toggles, and terminal maintenance. Backend modules append their own suffixes to the same menu when loaded.

The command @code{agents-start-or-switch} is the main entry point. If no sessions exist, it starts a new session through the selected backend. If sessions already exist, it opens the session switcher. Sessions receive stable home-row keys where possible and display backend icons next to their labels.

The command @code{agents-run-skill} discovers skills from every loaded backend that implements skill discovery. It disambiguates same-named skills by backend label, prompts for declared arguments when a skill frontmatter requests them, and dispatches the invocation to the selected backend. Claude and Codex backends scan global and project-local programmatic skill directories, using both @code{project-current} and nearby tool directories as project-root signals, so batch-only skills can be run from this command without being loaded into ordinary interactive agent sessions.

The command @code{agents-post-push-ci} runs the @code{post-push-ci} skill through the
selected backend with @samp{--no-push --commit HEAD}. Use it after a push has
already happened and you want an agent to inspect GitHub Actions failures, fix
the root cause, and continue the authorized CI closeout loop.

The commands @code{agents-handoff}, @code{agents-audit-project}, @code{agents-debug-backtrace}, @code{agents-exit}, and @code{agents-restart} resolve a backend from the current session buffer or by prompting. This lets the same keybinding operate on Claude or Codex sessions without separate wrapper commands.

@code{agents-before-exit-functions} can delay @code{agents-exit} for cleanup work.
Each function receives the resolved backend and session buffer; returning nil
aborts the exit.

Set @code{agents-before-exit-skill-name} and
@code{agents-before-exit-skill-directories} to submit an interactive skill before
exiting matching sessions. @code{agents-run-skill-before-exit} uses
@code{agents-skill-command-prefix-alist} so Claude sessions receive slash commands
and Codex sessions receive dollar-prefixed skill commands, then submits the
command and delays exit until the next @code{agents-exit} call.

@node Claude Code commands
@section Claude Code commands

@findex agents-claude-start-or-switch
@findex agents-claude-select-account
@findex agents-claude-init-account
@findex agents-claude-run-skill
@findex agents-claude-batch-todos
@findex agents-claude-send-todo-at-point
@findex agents-claude-audit-project
@findex agents-claude-debug-backtrace
@findex agents-claude-handoff
@findex agents-claude-restart
@findex agents-claude-switch-branch
@findex agents-claude-create-branch
@findex agents-claude-ensure-statusline-config
@findex agents-claude-ensure-stop-hook-config
@findex agents-claude-ensure-notification-hook-config
@findex agents-claude-setup-config
The Claude commands expose workflows that are specific to Claude Code. @code{agents-claude-start-or-switch} starts Claude under the selected account or opens the unified switcher. @code{agents-claude-select-account} persists an account choice, and @code{agents-claude-init-account} prepares a new account directory with shared configuration files and symlinks.

@code{agents-claude-run-skill}, @code{agents-claude-batch-todos}, and @code{agents-claude-send-todo-at-point} use non-interactive or interactive Claude sessions to run structured work. Batch TODO processing extracts Org headings, formats each heading and body as a prompt, runs entries sequentially, and records a summary buffer. Project audits refuse to auto-commit unless the target git worktree is clean before the audit starts.

@code{agents-claude-create-branch} creates a new Claude branch from the current session. With isolated branching it creates a git worktree under @code{agents-claude-fork-worktree-directory} and links the branch session metadata into that project. @code{agents-claude-switch-branch} displays the branch tree and switches to, or resumes, the selected session.

The setup command @code{agents-claude-setup-config} ensures that Claude's statusline, Stop hook, and Notification hook are present in @samp{~/.claude/settings.json}. The backend does not run setup automatically when loaded; run the setup command explicitly when installing or repairing hook configuration.

@node Codex commands
@section Codex commands

@findex agents-codex-start-or-switch
@findex agents-codex-select-account
@findex agents-codex-run-skill
@findex agents-codex-audit-project
@findex agents-codex-debug-backtrace
@findex agents-codex-handoff
@findex agents-codex-restart
@findex agents-codex-exit
@findex agents-codex-resume
@findex agents-codex-fork
@findex agents-codex-setup-kill-on-exit
The Codex commands mirror the shared workflows for the Codex CLI@. @code{agents-codex-start-or-switch} starts Codex under the selected account when no Codex sessions exist, otherwise it opens the shared switcher. @code{agents-codex-select-account} persists an account choice by name and applies it to new interactive sessions and non-interactive @samp{codex exec} runs. @code{agents-codex-run-skill} runs a skill non-interactively with @samp{codex exec}, using local skill metadata when available and falling back to a slash invocation otherwise. Additional programmatic skill directories are configured through @code{agents-codex-programmatic-skill-directories}.

@code{agents-codex-audit-project} prompts for a project directory and runs each configured audit skill sequentially through @samp{codex exec}. @code{agents-codex-debug-backtrace} saves the current Emacs backtrace, asks @samp{gptel} to identify likely packages, and starts a Codex session in the selected package source directory with instructions to fix the bug.

@code{agents-codex-handoff} reads @code{agents-codex-handoff-file}, kills the current Codex buffer, and starts a new session with the handoff prompt. @code{agents-codex-handoff-from-emacsclient} accepts the source buffer name from an @samp{emacsclient} argument, so agent-authored handoff commands can close the requesting session even though Emacs evaluates the form outside that buffer. @code{agents-codex-restart} resumes the current project with @samp{codex resume --last} after killing the old session. @code{agents-codex-resume} and @code{agents-codex-fork} expose the Codex CLI's resume and fork subcommands from the unified menu.

@node Functions
@chapter Functions

@menu
* Backend registration and display::
* Modeline and status helpers::
@end menu

@node Backend registration and display
@section Backend registration and display

@findex agents-register-backend
@findex agents-backend-icon
@findex agents-svg-icon
@findex agents-display-name
@findex agents-parse-skill-frontmatter
@findex agents-notify
@findex agents-protect-buffer
@findex agents-kill-session-buffer
The function @code{agents-register-backend} registers an AI tool by associating a backend symbol with a property list of buffer predicates, session discovery functions, command senders, and optional command handlers. Backend modules call it at load time. Required keys are validated so the shared session layer can rely on a consistent interface.

@code{agents-display-name} returns a human-readable label for a session buffer. It starts with the project name, adds an instance name when needed to disambiguate duplicates, and lets backends append their own suffixes. The Claude backend uses this to expose branch identity in the modeline and switcher.

@code{agents-parse-skill-frontmatter} reads @samp{SKILL.md} frontmatter shared by Claude and Codex skill runners. It recognizes names, descriptions, argument hints, argument sources, argument choices, defaults, multiplicity, user-invocable flags, and model overrides.

@code{agents-svg-icon} renders SVG strings as inline images, using the current face foreground when the SVG uses @samp{currentColor}. Backends use this for modeline and transient display and can fall back to text abbreviations when SVG rendering is unavailable.

@node Modeline and status helpers
@section Modeline and status helpers

@findex agents-claude-status-model
@findex agents-claude-status-cost
@findex agents-claude-status-context-percent
@findex agents-claude-status-token-count
@findex agents-claude-status-duration-ms
@findex agents-claude-status-session-usage
@findex agents-claude-status-weekly-usage
@findex agents-codex-status-model
@findex agents-codex-status-duration-ms
The Claude and Codex status helpers are intended for modeline integrations such as @samp{doom-modeline}. Claude status functions read the current session status JSON and expose model, cost, context, duration, and token information. Claude usage functions expose account-level utilization returned by the Anthropic usage endpoint. Codex status functions expose the configured model and elapsed session duration.

@node Transient menu
@chapter Transient menu

@findex agents-menu
Backend modules extend @code{agents-menu} through @code{transient-append-suffix} after the core menu is available, first removing their own existing suffixes so reloading does not duplicate or stale-position those entries. The base menu contains shared session, tool, alert, buffer, and option entries. The Claude module appends branch navigation, batch TODOs, TODO sending, log browsing, status polling, Claude account selection, and branch-kill warnings. The Codex module appends Codex resume/fork commands and Codex account selection. In the Options group, @samp{-c} selects the Claude account and @samp{-x} selects the Codex account so the two account selectors appear next to each other.

Boolean menu infixes use a shared transient class that toggles the bound variable each time it is invoked. The theme-sync infix additionally calls @code{agents-sync-theme-now} when enabling theme sync, so backend configuration files are updated immediately.

@node Indices
@chapter Indices

@menu
* Function index::
* Variable index::
@end menu

@node Function index
@section Function index

@printindex fn

@node Variable index
@section Variable index

@printindex vr

@bye
